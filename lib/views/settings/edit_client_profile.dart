import 'package:flutter/material.dart';

class EditClientProfile extends StatelessWidget {
  const EditClientProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Edit Client Profile"),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
