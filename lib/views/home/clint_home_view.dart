import 'package:flutter/material.dart';

class ClintHomeView extends StatelessWidget {
  const ClintHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}